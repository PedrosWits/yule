

giveMeAYule = function(lambda, n, leafNames) {
  
  
  yule = data.frame(Name   = as.character(1:n), 
                    Parent = parents,
                    UChild = uchilds,
                    LChild = lchilds,
                    Length = lengths)
  return()  
}